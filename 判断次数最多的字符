#include <stdio.h>
#include <string.h>
int main( )
{
    
    int cases, sum[10], i, max;
    //  组数   0-9 循环控制 每行的最大值
    char str[1000];
    //每行的字符串
    scanf("%d", &cases);
    //接受组数
    while (cases > 0)//5 4 321这种倒数各个行数，（按逻辑顺序输出）
        {
            scanf("%s", str);//得到该行字符串
            //gets(str);也是可以的
            for( i = 0; i < 10; i++)//给sum赋初始值
              sum[i] = 0;
            for(i = 0; i < strlen(str); i++)//提出字符并且计数
            sum[str[i]-'0']++;//每个数字对应自己的数了多少次
            //昨天那道题中有提到直接将ascall码中的数字变成int型的数字
        
            //比较sum数组的值的大小，用下标，也可以直接用数组里面的数字
            max = 0;//下标
            for (i = 0; i < 10; i++)
            {
                
                if (sum[i] > sum[max]) max = i;
                else if ((sum[i] == sum[max] )&& (i < max)) max = i;
                  //如果有多个字符出现的次数相同且最多，那么输出ASCII码最小的那个
            }
            printf("%c %d\n", max + '0', sum[max]);
            
            cases --;
            
        }
    
    
    
    
}

//程序改错。
以下程序的功能是统计字符数。判断一个由’0’ ~ ‘9’这10个字符组成的字符串中哪个字符出现的次数最多。
输入数据：第一行是测试数据的组数m，每组测试数据占1行，每行数据不超过1000个字符且非空。
输出要求：m行，每行对应一组输入，包括出现次数最多的字符和该字符出现的次数。如果有多个字符出现的次数相同且最多，那么输出ASCII码最小的那一个。//
